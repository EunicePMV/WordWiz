if __name__ == "__main__":
    from wordgen import sample
    from trie import alpha #Tried to use the alphabet, natae ung AI, need freq distrib heuristics
    # insert here the module to generate a random letters (MM)
    # in the generated random letters + MM letters, there should be generated 100 words 
    # mastermind generate a word that has less frequency of letters 
    sample("socky", "rockyaedius")